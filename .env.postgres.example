## Local PostgreSQL (Docker)
DATABASE_URL="postgresql://alo:alo@localhost:5432/alo?schema=public"

## NextAuth (local)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="local-dev-secret-change-me"

## Public site url (email linkleri için)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

## SMTP (Email gönderimi)
## Gönderici adresi olarak `destek@alo17.tr` kullanmak için genelde SMTP_USER=destek@alo17.tr olmalı
SMTP_HOST="mail.kurumsaleposta.com"
SMTP_PORT="587"
SMTP_USER="destek@alo17.tr"
SMTP_PASS="CHANGE_ME"
## Opsiyonel: bazı sunucularda relay hatasını önlemek için FROM ile USER aynı olmalı
SMTP_FROM="destek@alo17.tr"
## Opsiyonel: TLS doğrulama (sadece sorun giderme için false yapın)
SMTP_REJECT_UNAUTHORIZED="true"

## Opsiyonel: admin bildirimleri için
ADMIN_EMAIL="destek@alo17.tr"
## Opsiyonel: mail içeriklerinde destek adresi (varsayılan SMTP_USER)
SUPPORT_EMAIL="destek@alo17.tr"

## Optional encryption key for credential storage (recommended)
## 64 hex chars (32 bytes). Example:
## ENCRYPTION_KEY="0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"

## Windows Sync Agent (Electron) -> Panel API
## Agent bu token ile /api/admin/marketplaces/agent/* endpointlerini çağırır.
## Örnek üretim: 32+ karakter random bir string kullanın.
MARKETPLACE_AGENT_TOKEN="CHANGE_ME_VERY_LONG_RANDOM"

